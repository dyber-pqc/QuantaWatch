apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "quantawatch.fullname" . }}-config
  labels:
    {{- include "quantawatch.labels" . | nindent 4 }}
data:
  quantawatch.yaml: |
{{ .Values.config | indent 4 }}
